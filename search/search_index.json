{"config":{"lang":["es"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"\ud83c\udf7d\ufe0f Restaurante CLI","text":"<p>Sistema de gesti\u00f3n para restaurante \u2014 una aplicaci\u00f3n de l\u00ednea de comandos construida con Python, Typer y Rich.</p>"},{"location":"#que-es-restaurante-cli","title":"\u00bfQu\u00e9 es Restaurante CLI?","text":"<p>Restaurante CLI es una herramienta de terminal que permite gestionar las operaciones b\u00e1sicas de un restaurante: administrar meseros y el men\u00fa de platillos. Est\u00e1 dise\u00f1ada como proyecto acad\u00e9mico aplicando principios de c\u00f3digo limpio y arquitectura por capas.</p>"},{"location":"#caracteristicas-principales","title":"Caracter\u00edsticas principales","text":"<ul> <li>Gesti\u00f3n de meseros: registro, login con PIN, listado y eliminaci\u00f3n.</li> <li>CRUD de platillos: crear, listar, filtrar por categor\u00eda, actualizar y eliminar platillos del men\u00fa.</li> <li>Men\u00fa interactivo: interfaz visual en terminal con tablas, paneles y colores usando Rich.</li> <li>Comandos directos: tambi\u00e9n se puede usar con flags (<code>--id</code>, <code>--nombre</code>, etc.) sin pasar por el men\u00fa.</li> <li>Validaciones robustas: cada modelo valida sus datos al momento de crearse usando <code>__post_init__</code>.</li> <li>Persistencia dual: almacena datos en archivos <code>.json</code> locales o en una base de datos Supabase (PostgreSQL) seg\u00fan la configuraci\u00f3n del entorno.</li> </ul>"},{"location":"#arquitectura-general","title":"Arquitectura general","text":"<p>El proyecto sigue una arquitectura en capas con separaci\u00f3n clara de responsabilidades. La capa de Storage admite dos implementaciones intercambiables: JSON local y Supabase REST API.</p> <pre><code>flowchart LR\n    CLI[\"\ud83d\udda5\ufe0f CLI (Typer + Rich)\"]\n    SVC[\"\u2699\ufe0f Services\"]\n    STR[\"\ud83d\udcbe Storage\"]\n    JSON[\"\ud83d\udcc4 JSON Files\"]\n    SB[\"\u2601\ufe0f Supabase (PostgreSQL)\"]\n\n    CLI --&gt; SVC --&gt; STR\n    STR -. \"sin .env\" .-&gt; JSON\n    STR -. \"con .env\" .-&gt; SB</code></pre> Capa Responsabilidad CLI Interacci\u00f3n con el usuario (prompts, tablas, paneles) Services L\u00f3gica de negocio y validaciones de flujo Storage Lectura/escritura de datos \u2014 JSON local o Supabase REST API Models Definici\u00f3n de entidades con <code>dataclasses</code> y validaciones <p>\u00bfPrimera vez aqu\u00ed?</p> <p>Dir\u00edgete a la secci\u00f3n de Primeros pasos para instalar y ejecutar el proyecto.</p> <p>\u00bfQuieres conectar Supabase?</p> <p>Lee la secci\u00f3n de Persistencia de datos para configurar la base de datos en la nube.</p>"},{"location":"architecture/","title":"\ud83c\udfd7\ufe0f Arquitectura","text":"<p>Esta secci\u00f3n describe las decisiones de dise\u00f1o tomadas en el proyecto y los principios de c\u00f3digo limpio aplicados.</p>"},{"location":"architecture/#estructura-del-proyecto-src-layout","title":"Estructura del proyecto (src layout)","text":"<p>El proyecto utiliza el patr\u00f3n src layout, que separa el c\u00f3digo fuente de la configuraci\u00f3n y los tests:</p> <pre><code>restaurante-cli/\n\u251c\u2500\u2500 main.py                      # Punto de entrada (carga .env antes de importar)\n\u251c\u2500\u2500 pyproject.toml               # Configuraci\u00f3n del proyecto\n\u251c\u2500\u2500 schema.sql                   # DDL completo para crear las tablas en Supabase\n\u251c\u2500\u2500 .env                         # Variables de entorno (no se sube a git)\n\u251c\u2500\u2500 data/                        # Archivos JSON de persistencia local\n\u2502   \u251c\u2500\u2500 meseros.json\n\u2502   \u2514\u2500\u2500 platillos.json\n\u251c\u2500\u2500 src/\n\u2502   \u2514\u2500\u2500 mi_app/\n\u2502       \u251c\u2500\u2500 cli/                 # Capa de presentaci\u00f3n (Typer + Rich)\n\u2502       \u2502   \u251c\u2500\u2500 app.py\n\u2502       \u2502   \u251c\u2500\u2500 meseros.py\n\u2502       \u2502   \u2514\u2500\u2500 platillos.py\n\u2502       \u251c\u2500\u2500 models/              # Entidades del dominio (dataclasses)\n\u2502       \u2502   \u251c\u2500\u2500 mesero.py\n\u2502       \u2502   \u2514\u2500\u2500 platillo.py\n\u2502       \u251c\u2500\u2500 services.py          # L\u00f3gica de negocio\n\u2502       \u251c\u2500\u2500 storage.py           # Storage JSON (implementaci\u00f3n local)\n\u2502       \u251c\u2500\u2500 storage_supabase.py  # Storage Supabase (implementaci\u00f3n en la nube)\n\u2502       \u2514\u2500\u2500 exceptions.py        # Excepciones personalizadas\n\u251c\u2500\u2500 tests/\n\u2502   \u2514\u2500\u2500 test_services.py\n\u2514\u2500\u2500 docs/                        # Documentaci\u00f3n MkDocs\n</code></pre> <p>\u00bfPor qu\u00e9 src layout?</p> <p>Este patr\u00f3n evita que el c\u00f3digo fuente se importe accidentalmente desde la ra\u00edz del proyecto, obligando a usar instalaciones expl\u00edcitas. Es una pr\u00e1ctica recomendada por la comunidad Python.</p>"},{"location":"architecture/#separacion-por-capas","title":"Separaci\u00f3n por capas","text":"<p>El sistema est\u00e1 organizado en 4 capas con responsabilidades bien definidas. La capa de Storage tiene dos implementaciones concretas que los servicios no distinguen:</p> <pre><code>flowchart TD\n    subgraph Presentaci\u00f3n\n        CLI[\"cli/ \u2014 Typer + Rich\"]\n    end\n\n    subgraph Negocio\n        SVC[\"services.py \u2014 L\u00f3gica de negocio\"]\n        EXC[\"exceptions.py \u2014 Errores del dominio\"]\n    end\n\n    subgraph Dominio\n        MOD[\"models/ \u2014 Dataclasses\"]\n    end\n\n    subgraph Persistencia\n        PROTO[\"storage Protocol \u2014 contrato abstracto\"]\n        JSON[\"MeseroJSONStorage\\nPlatilloJSONStorage\"]\n        SB[\"MeseroSupabaseStorage\\nPlatilloSupabaseStorage\"]\n    end\n\n    CLI --&gt; SVC\n    SVC --&gt; MOD\n    SVC --&gt; PROTO\n    SVC --&gt; EXC\n    PROTO --&gt; JSON\n    PROTO --&gt; SB</code></pre>"},{"location":"architecture/#capa-cli-cli","title":"Capa CLI (<code>cli/</code>)","text":"<p>Maneja toda la interacci\u00f3n con el usuario: prompts, tablas Rich, paneles de \u00e9xito/error. No contiene l\u00f3gica de negocio; delega todo al servicio correspondiente. Al iniciar, decide qu\u00e9 implementaci\u00f3n de Storage usar: si las variables <code>SUPABASE_URL</code> y <code>SUPABASE_KEY</code> est\u00e1n definidas en el entorno, instancia el storage de Supabase; de lo contrario, usa JSON local.</p>"},{"location":"architecture/#capa-de-servicios-servicespy","title":"Capa de Servicios (<code>services.py</code>)","text":"<p>Contiene las reglas de negocio: verificar duplicados, buscar por ID, validar credenciales. Recibe un objeto <code>Storage</code> por inyecci\u00f3n en el constructor y no sabe si est\u00e1 hablando con archivos JSON o con Supabase.</p>"},{"location":"architecture/#capa-de-modelos-models","title":"Capa de Modelos (<code>models/</code>)","text":"<p>Define las entidades <code>Mesero</code> y <code>Platillo</code> como <code>dataclasses</code> con validaciones en <code>__post_init__</code>. Cada entidad vive en su propio archivo.</p>"},{"location":"architecture/#capa-de-storage-storagepy-storage_supabasepy","title":"Capa de Storage (<code>storage.py</code> / <code>storage_supabase.py</code>)","text":"<p>Implementa la lectura/escritura de datos. Usa <code>Protocol</code> para definir el contrato y permite intercambiar implementaciones sin tocar los servicios.</p> Implementaci\u00f3n Mecanismo Cu\u00e1ndo se usa <code>MeseroJSONStorage</code> Archivo <code>data/meseros.json</code> Sin <code>.env</code> configurado <code>MeseroSupabaseStorage</code> API REST de Supabase (PostgREST) Con <code>SUPABASE_URL</code> y <code>SUPABASE_KEY</code> en <code>.env</code> <code>PlatilloJSONStorage</code> Archivo <code>data/platillos.json</code> Sin <code>.env</code> configurado <code>PlatilloSupabaseStorage</code> API REST de Supabase (PostgREST) Con <code>SUPABASE_URL</code> y <code>SUPABASE_KEY</code> en <code>.env</code>"},{"location":"architecture/#principios-de-codigo-limpio-aplicados","title":"Principios de c\u00f3digo limpio aplicados","text":""},{"location":"architecture/#single-responsibility-principle-srp","title":"Single Responsibility Principle (SRP)","text":"<p>Cada m\u00f3dulo tiene una \u00fanica responsabilidad:</p> <ul> <li><code>models/</code> \u2192 solo define la estructura y validaci\u00f3n de datos.</li> <li><code>services.py</code> \u2192 solo contiene l\u00f3gica de negocio.</li> <li><code>storage.py</code> / <code>storage_supabase.py</code> \u2192 solo manejan persistencia.</li> <li><code>cli/</code> \u2192 solo maneja la interfaz de usuario.</li> </ul>"},{"location":"architecture/#dependency-inversion","title":"Dependency Inversion","text":"<p>Los servicios dependen de abstracciones (<code>Protocol</code>), no de implementaciones concretas. Esto permite usar mocks en los tests y cambiar el backend de almacenamiento sin tocar la l\u00f3gica de negocio.</p>"},{"location":"architecture/#openclosed-principle","title":"Open/Closed Principle","text":"<p>El sistema est\u00e1 abierto para extensi\u00f3n (se puede agregar <code>MeseroSQLiteStorage</code>, <code>MeseroRedisStorage</code>, etc.) sin modificar los servicios existentes \u2014 solo hay que respetar el <code>Protocol</code>.</p>"},{"location":"architecture/#dont-repeat-yourself-dry","title":"Don't Repeat Yourself (DRY)","text":"<p>Los helpers de UI (<code>_ok</code>, <code>_error</code>, <code>_tabla_meseros</code>, <code>_tabla_platillos</code>) encapsulan patrones repetidos de presentaci\u00f3n, evitando duplicar c\u00f3digo de Rich en cada comando. En <code>storage_supabase.py</code>, <code>_base()</code>, <code>_headers()</code> y <code>_check()</code> centralizan la configuraci\u00f3n HTTP para evitar repetici\u00f3n entre las dos clases de storage.</p>"},{"location":"architecture/#fail-fast","title":"Fail Fast","text":"<p>Los modelos validan sus datos inmediatamente al crearse gracias a <code>__post_init__</code>. Si un dato es inv\u00e1lido, se lanza una excepci\u00f3n antes de que el objeto entre en el sistema.</p> <p>Ejemplo de validaci\u00f3n temprana</p> <pre><code>@dataclass\nclass Mesero:\n    mesero_id: int\n    nombre: str\n    pin: str\n\n    def __post_init__(self) -&gt; None:\n        self._validar_id()\n        self._validar_nombre()\n        self._validar_pin()\n</code></pre>"},{"location":"architecture/#jerarquia-de-excepciones","title":"Jerarqu\u00eda de excepciones","text":"<p>Las excepciones siguen una jerarqu\u00eda que facilita el manejo de errores:</p> <pre><code>classDiagram\n    AppError &lt;|-- MeseroError\n    AppError &lt;|-- PlatilloError\n    MeseroError &lt;|-- MeseroNoEncontradoError\n    MeseroError &lt;|-- MeseroYaExisteError\n    MeseroError &lt;|-- DatosMeseroInvalidosError\n    PlatilloError &lt;|-- PlatilloNoEncontradoError\n    PlatilloError &lt;|-- PlatilloYaExisteError\n    PlatilloError &lt;|-- DatosPlatilloInvalidosError\n    PlatilloError &lt;|-- CategoriaInvalidaError</code></pre> <p>\u00bfPor qu\u00e9 una jerarqu\u00eda?</p> <p>Permite capturar errores de forma granular (<code>MeseroNoEncontradoError</code>) o general (<code>AppError</code>) seg\u00fan el contexto.</p>"},{"location":"getting-started/","title":"\ud83d\ude80 Primeros pasos","text":"<p>Esta gu\u00eda te ayudar\u00e1 a instalar y ejecutar el proyecto en tu m\u00e1quina local.</p>"},{"location":"getting-started/#requisitos-previos","title":"Requisitos previos","text":"<ul> <li>Python 3.12 o superior</li> <li>uv \u2014 gestor de paquetes y entornos virtuales</li> </ul> <p>\u00bfNo tienes uv instalado?</p> <p>Puedes instalarlo siguiendo las instrucciones oficiales en docs.astral.sh/uv.</p>"},{"location":"getting-started/#instalacion","title":"Instalaci\u00f3n","text":""},{"location":"getting-started/#1-clonar-el-repositorio","title":"1. Clonar el repositorio","text":"<pre><code>git clone https://github.com/tu-usuario/restaurante-cli.git\ncd restaurante-cli\n</code></pre>"},{"location":"getting-started/#2-instalar-dependencias","title":"2. Instalar dependencias","text":"MacOS / LinuxWindows <pre><code>uv sync\n</code></pre> <pre><code>uv sync\n</code></pre> <p>Esto crear\u00e1 el entorno virtual y descargar\u00e1 todas las dependencias definidas en <code>pyproject.toml</code>.</p>"},{"location":"getting-started/#primer-comando","title":"Primer comando","text":"<p>Para verificar que todo funciona correctamente, ejecuta:</p> <pre><code>uv run python main.py --help\n</code></pre> <p>Deber\u00edas ver la ayuda del CLI con los subcomandos disponibles:</p> <pre><code> Usage: main.py [OPTIONS] COMMAND [ARGS]...\n\n \ud83c\udf7d\ufe0f  Sistema de gesti\u00f3n del restaurante\n\n\u256d\u2500 Commands \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256e\n\u2502 menu        Abre el men\u00fa interactivo    \u2502\n\u2502 meseros     \ud83d\udc64  Gesti\u00f3n de meseros       \u2502\n\u2502 platillos   \ud83c\udf72  Gesti\u00f3n del men\u00fa         \u2502\n\u2570\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256f\n</code></pre>"},{"location":"getting-started/#abrir-el-menu-interactivo","title":"Abrir el men\u00fa interactivo","text":"<pre><code>uv run python main.py menu\n</code></pre> <p>\u00a1Listo!</p> <p>Si ves el men\u00fa principal con las opciones de meseros y platillos, el proyecto est\u00e1 correctamente instalado. Por defecto los datos se guardan en <code>data/meseros.json</code> y <code>data/platillos.json</code>.</p>"},{"location":"getting-started/#configurar-supabase-opcional","title":"Configurar Supabase (opcional)","text":"<p>Si quieres persistir los datos en la nube usando Supabase + PostgreSQL, sigue estos pasos adicionales:</p>"},{"location":"getting-started/#1-crear-las-tablas-en-supabase","title":"1. Crear las tablas en Supabase","text":"<p>En el panel de Supabase, ve a SQL Editor y ejecuta el contenido completo de <code>schema.sql</code> (incluido en la ra\u00edz del proyecto). Esto crear\u00e1 las tablas <code>meseros</code>, <code>platillos</code>, <code>mesas</code>, <code>pedidos</code> y <code>detalle_pedido</code> con todos sus datos de prueba.</p>"},{"location":"getting-started/#2-configurar-las-credenciales","title":"2. Configurar las credenciales","text":"<p>Edita el archivo <code>.env</code> en la ra\u00edz del proyecto y completa tus credenciales:</p> <pre><code># .env\nSUPABASE_URL=https://[TU_REF].supabase.co\nSUPABASE_KEY=[TU_ANON_KEY_O_SERVICE_ROLE_KEY]\n</code></pre> <p>Puedes encontrar estos valores en Supabase \u2192 Settings \u2192 API.</p> <p>Seguridad</p> <p>El archivo <code>.env</code> est\u00e1 incluido en <code>.gitignore</code> y nunca debe subirse al repositorio. Nunca compartas tu <code>service_role key</code> de forma p\u00fablica.</p>"},{"location":"getting-started/#3-ejecutar-con-supabase","title":"3. Ejecutar con Supabase","text":"<p>Simplemente ejecuta la aplicaci\u00f3n normalmente; detectar\u00e1 las credenciales autom\u00e1ticamente:</p> <pre><code>uv run python main.py menu\n</code></pre> <p>\u00bfC\u00f3mo s\u00e9 qu\u00e9 backend est\u00e1 activo?</p> <p>Si <code>SUPABASE_URL</code> y <code>SUPABASE_KEY</code> est\u00e1n definidos en <code>.env</code>, la app usa Supabase. Si no est\u00e1n, usa los archivos JSON locales. No hay ning\u00fan flag adicional que cambiar.</p>"},{"location":"getting-started/#ejecutar-los-tests","title":"Ejecutar los tests","text":"<pre><code>uv run pytest -v\n</code></pre>"},{"location":"getting-started/#verificar-calidad-del-codigo","title":"Verificar calidad del c\u00f3digo","text":"LintComplejidad ciclom\u00e1tica <pre><code>uv run ruff check .\n</code></pre> <pre><code>uv run radon cc src -a\n</code></pre> <p>Complejidad</p> <p>El proyecto debe mantener una calificaci\u00f3n promedio de A en complejidad ciclom\u00e1tica.</p>"},{"location":"reference/","title":"\ud83d\udcda Referencia API","text":"<p>Documentaci\u00f3n generada autom\u00e1ticamente a partir de los docstrings del c\u00f3digo fuente.</p>"},{"location":"reference/#modelos","title":"Modelos","text":""},{"location":"reference/#mesero","title":"Mesero","text":""},{"location":"reference/#src.mi_app.models.mesero.Mesero","title":"<code>src.mi_app.models.mesero.Mesero</code>  <code>dataclass</code>","text":"<p>Representa un mesero registrado en el sistema del restaurante.</p> <p>Attributes:</p> Name Type Description <code>nombre</code> <code>str</code> <p>Nombre completo del mesero.</p> <code>pin</code> <code>str</code> <p>Clave num\u00e9rica de 4 d\u00edgitos para autenticaci\u00f3n.</p> <code>mesero_id</code> <code>int | None</code> <p>Identificador \u00fanico asignado autom\u00e1ticamente por el almacenamiento.</p> Source code in <code>src/mi_app/models/mesero.py</code> <pre><code>@dataclass\nclass Mesero:\n    \"\"\"Representa un mesero registrado en el sistema del restaurante.\n\n    Attributes:\n        nombre: Nombre completo del mesero.\n        pin: Clave num\u00e9rica de 4 d\u00edgitos para autenticaci\u00f3n.\n        mesero_id: Identificador \u00fanico asignado autom\u00e1ticamente por el almacenamiento.\n    \"\"\"\n\n    nombre: str\n    pin: str\n    mesero_id: int | None = None\n\n    def __post_init__(self) -&gt; None:\n        \"\"\"Valida los datos del mesero al momento de crearlo.\"\"\"\n        self._validar_id()\n        self._validar_nombre()\n        self._validar_pin()\n\n    def _validar_id(self) -&gt; None:\n        \"\"\"Verifica que el id, si ya fue asignado, sea un entero positivo.\"\"\"\n        if self.mesero_id is not None and self.mesero_id &lt;= 0:\n            raise DatosMeseroInvalidosError(\"El id del mesero debe ser un entero positivo\")\n\n    def _validar_nombre(self) -&gt; None:\n        \"\"\"Verifica que el nombre no est\u00e9 vac\u00edo.\"\"\"\n        if not self.nombre.strip():\n            raise DatosMeseroInvalidosError(\"El nombre del mesero no puede estar vac\u00edo\")\n\n    def _validar_pin(self) -&gt; None:\n        \"\"\"Verifica que el pin tenga exactamente 4 d\u00edgitos num\u00e9ricos.\"\"\"\n        if not self.pin.isdigit() or len(self.pin) != 4:\n            raise DatosMeseroInvalidosError(\"El pin debe tener exactamente 4 d\u00edgitos num\u00e9ricos\")\n</code></pre>"},{"location":"reference/#src.mi_app.models.mesero.Mesero.__post_init__","title":"<code>__post_init__()</code>","text":"<p>Valida los datos del mesero al momento de crearlo.</p> Source code in <code>src/mi_app/models/mesero.py</code> <pre><code>def __post_init__(self) -&gt; None:\n    \"\"\"Valida los datos del mesero al momento de crearlo.\"\"\"\n    self._validar_id()\n    self._validar_nombre()\n    self._validar_pin()\n</code></pre>"},{"location":"reference/#platillo","title":"Platillo","text":""},{"location":"reference/#src.mi_app.models.platillo.Platillo","title":"<code>src.mi_app.models.platillo.Platillo</code>  <code>dataclass</code>","text":"<p>Representa un platillo del men\u00fa del restaurante.</p> <p>Attributes:</p> Name Type Description <code>nombre</code> <code>str</code> <p>Nombre del platillo.</p> <code>precio</code> <code>float</code> <p>Precio en pesos colombianos.</p> <code>categoria</code> <code>str</code> <p>Tipo de platillo (entrada, plato_fuerte, postre, bebida).</p> <code>disponible</code> <code>bool</code> <p>Indica si el platillo est\u00e1 disponible en el men\u00fa.</p> <code>platillo_id</code> <code>int | None</code> <p>Identificador \u00fanico asignado autom\u00e1ticamente por el almacenamiento.</p> Source code in <code>src/mi_app/models/platillo.py</code> <pre><code>@dataclass\nclass Platillo:\n    \"\"\"Representa un platillo del men\u00fa del restaurante.\n\n    Attributes:\n        nombre: Nombre del platillo.\n        precio: Precio en pesos colombianos.\n        categoria: Tipo de platillo (entrada, plato_fuerte, postre, bebida).\n        disponible: Indica si el platillo est\u00e1 disponible en el men\u00fa.\n        platillo_id: Identificador \u00fanico asignado autom\u00e1ticamente por el almacenamiento.\n    \"\"\"\n\n    nombre: str\n    precio: float\n    categoria: str\n    disponible: bool = True\n    platillo_id: int | None = None\n\n    def __post_init__(self) -&gt; None:\n        \"\"\"Valida los datos del platillo al momento de crearlo.\"\"\"\n        self._validar_id()\n        self._validar_nombre()\n        self._validar_precio()\n        self._validar_categoria()\n\n    def _validar_id(self) -&gt; None:\n        \"\"\"Verifica que el id, si ya fue asignado, sea un entero positivo.\"\"\"\n        if self.platillo_id is not None and self.platillo_id &lt;= 0:\n            raise DatosPlatilloInvalidosError(\"El id del platillo debe ser un entero positivo\")\n\n    def _validar_nombre(self) -&gt; None:\n        \"\"\"Verifica que el nombre no est\u00e9 vac\u00edo.\"\"\"\n        if not self.nombre.strip():\n            raise DatosPlatilloInvalidosError(\"El nombre del platillo no puede estar vac\u00edo\")\n\n    def _validar_precio(self) -&gt; None:\n        \"\"\"Verifica que el precio sea mayor a cero.\"\"\"\n        if self.precio &lt;= 0:\n            raise DatosPlatilloInvalidosError(\"El precio debe ser mayor a cero\")\n\n    def _validar_categoria(self) -&gt; None:\n        \"\"\"Verifica que la categor\u00eda sea una de las permitidas.\"\"\"\n        if self.categoria not in CATEGORIAS_VALIDAS:\n            raise CategoriaInvalidaError(self.categoria)\n</code></pre>"},{"location":"reference/#src.mi_app.models.platillo.Platillo.__post_init__","title":"<code>__post_init__()</code>","text":"<p>Valida los datos del platillo al momento de crearlo.</p> Source code in <code>src/mi_app/models/platillo.py</code> <pre><code>def __post_init__(self) -&gt; None:\n    \"\"\"Valida los datos del platillo al momento de crearlo.\"\"\"\n    self._validar_id()\n    self._validar_nombre()\n    self._validar_precio()\n    self._validar_categoria()\n</code></pre>"},{"location":"reference/#servicios","title":"Servicios","text":""},{"location":"reference/#meseroservice","title":"MeseroService","text":""},{"location":"reference/#src.mi_app.services.MeseroService","title":"<code>src.mi_app.services.MeseroService</code>","text":"<p>Contiene la l\u00f3gica de negocio para la gesti\u00f3n de meseros.</p> Source code in <code>src/mi_app/services.py</code> <pre><code>class MeseroService:\n    \"\"\"Contiene la l\u00f3gica de negocio para la gesti\u00f3n de meseros.\"\"\"\n\n    def __init__(self, storage: MeseroStorage) -&gt; None:\n        \"\"\"Inicializa el servicio con su capa de almacenamiento.\"\"\"\n        self.storage = storage\n\n    # \u2500\u2500 Operaciones p\u00fablicas \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    def registrar_mesero(self, mesero: Mesero) -&gt; None:\n        \"\"\"Registra un nuevo mesero si no existe uno con el mismo id.\"\"\"\n        meseros = self.storage.cargar()\n        self._verificar_que_no_existe(mesero.mesero_id, meseros)\n        self._persistir_mesero(mesero, meseros)\n\n    def login(self, mesero_id: int, pin: str) -&gt; Mesero:\n        \"\"\"Valida las credenciales del mesero y lo retorna si son correctas.\"\"\"\n        meseros = self.storage.cargar()\n        mesero = self._buscar_por_id(mesero_id, meseros)\n        self._verificar_pin(pin, mesero)\n        return mesero\n\n    def obtener_mesero(self, mesero_id: int) -&gt; Mesero:\n        \"\"\"Retorna el mesero con el id dado o lanza error si no existe.\"\"\"\n        meseros = self.storage.cargar()\n        return self._buscar_por_id(mesero_id, meseros)\n\n    def listar_meseros(self) -&gt; list[Mesero]:\n        \"\"\"Retorna la lista completa de meseros registrados.\"\"\"\n        return self.storage.cargar()\n\n    def eliminar_mesero(self, mesero_id: int) -&gt; None:\n        \"\"\"Elimina el mesero con el id dado o lanza error si no existe.\"\"\"\n        meseros = self.storage.cargar()\n        filtrados = [m for m in meseros if m.mesero_id != mesero_id]\n\n        if len(filtrados) == len(meseros):\n            raise MeseroNoEncontradoError(mesero_id)\n\n        self.storage.guardar(filtrados)\n\n    # \u2500\u2500 Helpers privados \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    def _buscar_por_id(self, mesero_id: int, meseros: list[Mesero]) -&gt; Mesero:\n        \"\"\"Busca un mesero por id dentro de una lista ya cargada.\"\"\"\n        for mesero in meseros:\n            if mesero.mesero_id == mesero_id:\n                return mesero\n        raise MeseroNoEncontradoError(mesero_id)\n\n    def _verificar_que_no_existe(self, mesero_id: int | None, meseros: list[Mesero]) -&gt; None:\n        \"\"\"Lanza error si ya existe un mesero con el id dado.\"\"\"\n        if mesero_id is not None and any(m.mesero_id == mesero_id for m in meseros):\n            raise MeseroYaExisteError(mesero_id)\n\n    def _verificar_pin(self, pin: str, mesero: Mesero) -&gt; None:\n        \"\"\"Lanza error si el pin no coincide con el del mesero.\"\"\"\n        if mesero.pin != pin:\n            raise DatosMeseroInvalidosError(\"Pin incorrecto\")\n\n    def _persistir_mesero(self, mesero: Mesero, meseros: list[Mesero]) -&gt; None:\n        \"\"\"Agrega el mesero a la lista y la guarda en storage.\"\"\"\n        meseros.append(mesero)\n        self.storage.guardar(meseros)\n</code></pre>"},{"location":"reference/#src.mi_app.services.MeseroService.__init__","title":"<code>__init__(storage)</code>","text":"<p>Inicializa el servicio con su capa de almacenamiento.</p> Source code in <code>src/mi_app/services.py</code> <pre><code>def __init__(self, storage: MeseroStorage) -&gt; None:\n    \"\"\"Inicializa el servicio con su capa de almacenamiento.\"\"\"\n    self.storage = storage\n</code></pre>"},{"location":"reference/#src.mi_app.services.MeseroService.eliminar_mesero","title":"<code>eliminar_mesero(mesero_id)</code>","text":"<p>Elimina el mesero con el id dado o lanza error si no existe.</p> Source code in <code>src/mi_app/services.py</code> <pre><code>def eliminar_mesero(self, mesero_id: int) -&gt; None:\n    \"\"\"Elimina el mesero con el id dado o lanza error si no existe.\"\"\"\n    meseros = self.storage.cargar()\n    filtrados = [m for m in meseros if m.mesero_id != mesero_id]\n\n    if len(filtrados) == len(meseros):\n        raise MeseroNoEncontradoError(mesero_id)\n\n    self.storage.guardar(filtrados)\n</code></pre>"},{"location":"reference/#src.mi_app.services.MeseroService.listar_meseros","title":"<code>listar_meseros()</code>","text":"<p>Retorna la lista completa de meseros registrados.</p> Source code in <code>src/mi_app/services.py</code> <pre><code>def listar_meseros(self) -&gt; list[Mesero]:\n    \"\"\"Retorna la lista completa de meseros registrados.\"\"\"\n    return self.storage.cargar()\n</code></pre>"},{"location":"reference/#src.mi_app.services.MeseroService.login","title":"<code>login(mesero_id, pin)</code>","text":"<p>Valida las credenciales del mesero y lo retorna si son correctas.</p> Source code in <code>src/mi_app/services.py</code> <pre><code>def login(self, mesero_id: int, pin: str) -&gt; Mesero:\n    \"\"\"Valida las credenciales del mesero y lo retorna si son correctas.\"\"\"\n    meseros = self.storage.cargar()\n    mesero = self._buscar_por_id(mesero_id, meseros)\n    self._verificar_pin(pin, mesero)\n    return mesero\n</code></pre>"},{"location":"reference/#src.mi_app.services.MeseroService.obtener_mesero","title":"<code>obtener_mesero(mesero_id)</code>","text":"<p>Retorna el mesero con el id dado o lanza error si no existe.</p> Source code in <code>src/mi_app/services.py</code> <pre><code>def obtener_mesero(self, mesero_id: int) -&gt; Mesero:\n    \"\"\"Retorna el mesero con el id dado o lanza error si no existe.\"\"\"\n    meseros = self.storage.cargar()\n    return self._buscar_por_id(mesero_id, meseros)\n</code></pre>"},{"location":"reference/#src.mi_app.services.MeseroService.registrar_mesero","title":"<code>registrar_mesero(mesero)</code>","text":"<p>Registra un nuevo mesero si no existe uno con el mismo id.</p> Source code in <code>src/mi_app/services.py</code> <pre><code>def registrar_mesero(self, mesero: Mesero) -&gt; None:\n    \"\"\"Registra un nuevo mesero si no existe uno con el mismo id.\"\"\"\n    meseros = self.storage.cargar()\n    self._verificar_que_no_existe(mesero.mesero_id, meseros)\n    self._persistir_mesero(mesero, meseros)\n</code></pre>"},{"location":"reference/#platilloservice","title":"PlatilloService","text":""},{"location":"reference/#src.mi_app.services.PlatilloService","title":"<code>src.mi_app.services.PlatilloService</code>","text":"<p>Contiene la l\u00f3gica de negocio para el CRUD del men\u00fa de platillos.</p> Source code in <code>src/mi_app/services.py</code> <pre><code>class PlatilloService:\n    \"\"\"Contiene la l\u00f3gica de negocio para el CRUD del men\u00fa de platillos.\"\"\"\n\n    def __init__(self, storage: PlatilloStorage) -&gt; None:\n        \"\"\"Inicializa el servicio con su capa de almacenamiento.\"\"\"\n        self.storage = storage\n\n    # \u2500\u2500 Operaciones p\u00fablicas \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    def crear_platillo(self, platillo: Platillo) -&gt; None:\n        \"\"\"Agrega un platillo al men\u00fa si no existe uno con el mismo id.\"\"\"\n        platillos = self.storage.cargar()\n        self._verificar_que_no_existe(platillo.platillo_id, platillos)\n        self._persistir_platillo(platillo, platillos)\n\n    def obtener_platillo(self, platillo_id: int) -&gt; Platillo:\n        \"\"\"Retorna el platillo con el id dado o lanza error si no existe.\"\"\"\n        platillos = self.storage.cargar()\n        return self._buscar_por_id(platillo_id, platillos)\n\n    def listar_platillos(self) -&gt; list[Platillo]:\n        \"\"\"Retorna todos los platillos del men\u00fa.\"\"\"\n        return self.storage.cargar()\n\n    def listar_por_categoria(self, categoria: str) -&gt; list[Platillo]:\n        \"\"\"Retorna los platillos filtrados por categor\u00eda.\"\"\"\n        platillos = self.storage.cargar()\n        return [p for p in platillos if p.categoria == categoria]\n\n    def actualizar_platillo(\n        self, platillo_id: int, nombre: str, precio: float, categoria: str\n    ) -&gt; None:\n        \"\"\"Actualiza los datos de un platillo existente.\"\"\"\n        platillos = self.storage.cargar()\n        platillo = self._buscar_por_id(platillo_id, platillos)\n        self._aplicar_cambios(platillo, nombre, precio, categoria)\n        self.storage.guardar(platillos)\n\n    def eliminar_platillo(self, platillo_id: int) -&gt; None:\n        \"\"\"Elimina el platillo con el id dado o lanza error si no existe.\"\"\"\n        platillos = self.storage.cargar()\n        filtrados = [p for p in platillos if p.platillo_id != platillo_id]\n\n        if len(filtrados) == len(platillos):\n            raise PlatilloNoEncontradoError(platillo_id)\n\n        self.storage.guardar(filtrados)\n\n    # \u2500\u2500 Helpers privados \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    def _buscar_por_id(self, platillo_id: int, platillos: list[Platillo]) -&gt; Platillo:\n        \"\"\"Busca un platillo por id dentro de una lista ya cargada.\"\"\"\n        for platillo in platillos:\n            if platillo.platillo_id == platillo_id:\n                return platillo\n        raise PlatilloNoEncontradoError(platillo_id)\n\n    def _verificar_que_no_existe(self, platillo_id: int | None, platillos: list[Platillo]) -&gt; None:\n        \"\"\"Lanza error si ya existe un platillo con el id dado.\"\"\"\n        if platillo_id is not None and any(p.platillo_id == platillo_id for p in platillos):\n            raise PlatilloYaExisteError(platillo_id)\n\n    def _aplicar_cambios(\n        self, platillo: Platillo, nombre: str, precio: float, categoria: str\n    ) -&gt; None:\n        \"\"\"Aplica los nuevos valores al platillo (sin crear uno nuevo).\"\"\"\n        platillo.nombre = nombre.strip()\n        platillo.precio = precio\n        platillo.categoria = categoria\n\n    def _persistir_platillo(self, platillo: Platillo, platillos: list[Platillo]) -&gt; None:\n        \"\"\"Agrega el platillo a la lista y la guarda en storage.\"\"\"\n        platillos.append(platillo)\n        self.storage.guardar(platillos)\n</code></pre>"},{"location":"reference/#src.mi_app.services.PlatilloService.__init__","title":"<code>__init__(storage)</code>","text":"<p>Inicializa el servicio con su capa de almacenamiento.</p> Source code in <code>src/mi_app/services.py</code> <pre><code>def __init__(self, storage: PlatilloStorage) -&gt; None:\n    \"\"\"Inicializa el servicio con su capa de almacenamiento.\"\"\"\n    self.storage = storage\n</code></pre>"},{"location":"reference/#src.mi_app.services.PlatilloService.actualizar_platillo","title":"<code>actualizar_platillo(platillo_id, nombre, precio, categoria)</code>","text":"<p>Actualiza los datos de un platillo existente.</p> Source code in <code>src/mi_app/services.py</code> <pre><code>def actualizar_platillo(\n    self, platillo_id: int, nombre: str, precio: float, categoria: str\n) -&gt; None:\n    \"\"\"Actualiza los datos de un platillo existente.\"\"\"\n    platillos = self.storage.cargar()\n    platillo = self._buscar_por_id(platillo_id, platillos)\n    self._aplicar_cambios(platillo, nombre, precio, categoria)\n    self.storage.guardar(platillos)\n</code></pre>"},{"location":"reference/#src.mi_app.services.PlatilloService.crear_platillo","title":"<code>crear_platillo(platillo)</code>","text":"<p>Agrega un platillo al men\u00fa si no existe uno con el mismo id.</p> Source code in <code>src/mi_app/services.py</code> <pre><code>def crear_platillo(self, platillo: Platillo) -&gt; None:\n    \"\"\"Agrega un platillo al men\u00fa si no existe uno con el mismo id.\"\"\"\n    platillos = self.storage.cargar()\n    self._verificar_que_no_existe(platillo.platillo_id, platillos)\n    self._persistir_platillo(platillo, platillos)\n</code></pre>"},{"location":"reference/#src.mi_app.services.PlatilloService.eliminar_platillo","title":"<code>eliminar_platillo(platillo_id)</code>","text":"<p>Elimina el platillo con el id dado o lanza error si no existe.</p> Source code in <code>src/mi_app/services.py</code> <pre><code>def eliminar_platillo(self, platillo_id: int) -&gt; None:\n    \"\"\"Elimina el platillo con el id dado o lanza error si no existe.\"\"\"\n    platillos = self.storage.cargar()\n    filtrados = [p for p in platillos if p.platillo_id != platillo_id]\n\n    if len(filtrados) == len(platillos):\n        raise PlatilloNoEncontradoError(platillo_id)\n\n    self.storage.guardar(filtrados)\n</code></pre>"},{"location":"reference/#src.mi_app.services.PlatilloService.listar_platillos","title":"<code>listar_platillos()</code>","text":"<p>Retorna todos los platillos del men\u00fa.</p> Source code in <code>src/mi_app/services.py</code> <pre><code>def listar_platillos(self) -&gt; list[Platillo]:\n    \"\"\"Retorna todos los platillos del men\u00fa.\"\"\"\n    return self.storage.cargar()\n</code></pre>"},{"location":"reference/#src.mi_app.services.PlatilloService.listar_por_categoria","title":"<code>listar_por_categoria(categoria)</code>","text":"<p>Retorna los platillos filtrados por categor\u00eda.</p> Source code in <code>src/mi_app/services.py</code> <pre><code>def listar_por_categoria(self, categoria: str) -&gt; list[Platillo]:\n    \"\"\"Retorna los platillos filtrados por categor\u00eda.\"\"\"\n    platillos = self.storage.cargar()\n    return [p for p in platillos if p.categoria == categoria]\n</code></pre>"},{"location":"reference/#src.mi_app.services.PlatilloService.obtener_platillo","title":"<code>obtener_platillo(platillo_id)</code>","text":"<p>Retorna el platillo con el id dado o lanza error si no existe.</p> Source code in <code>src/mi_app/services.py</code> <pre><code>def obtener_platillo(self, platillo_id: int) -&gt; Platillo:\n    \"\"\"Retorna el platillo con el id dado o lanza error si no existe.\"\"\"\n    platillos = self.storage.cargar()\n    return self._buscar_por_id(platillo_id, platillos)\n</code></pre>"},{"location":"reference/#storage-json-local","title":"Storage \u2014 JSON (local)","text":""},{"location":"reference/#meserojsonstorage","title":"MeseroJSONStorage","text":""},{"location":"reference/#src.mi_app.storage.MeseroJSONStorage","title":"<code>src.mi_app.storage.MeseroJSONStorage</code>","text":"<p>Lee y escribe meseros desde/hacia un archivo JSON.</p> Source code in <code>src/mi_app/storage.py</code> <pre><code>class MeseroJSONStorage:\n    \"\"\"Lee y escribe meseros desde/hacia un archivo JSON.\"\"\"\n\n    def __init__(self, filepath: Path) -&gt; None:\n        \"\"\"Inicializa el storage con la ruta del archivo JSON.\"\"\"\n        self.filepath = filepath\n\n    def cargar(self) -&gt; list[Mesero]:\n        \"\"\"Carga la lista de meseros desde el archivo JSON.\"\"\"\n        if not self.filepath.exists():\n            return []\n\n        with open(self.filepath, \"r\", encoding=\"utf-8\") as f:\n            data = json.load(f)\n\n        return [Mesero(**item) for item in data]\n\n    def guardar(self, meseros: list[Mesero]) -&gt; None:\n        \"\"\"Persiste la lista de meseros en el archivo JSON.\n\n        Asigna autom\u00e1ticamente un ``mesero_id`` a los registros nuevos\n        cuyo id todav\u00eda sea ``None``, tomando como base el mayor id existente.\n        \"\"\"\n        ids_asignados = {m.mesero_id for m in meseros if m.mesero_id is not None}\n        siguiente_id = max(ids_asignados, default=0) + 1\n        for m in meseros:\n            if m.mesero_id is None:\n                m.mesero_id = siguiente_id\n                siguiente_id += 1\n\n        with open(self.filepath, \"w\", encoding=\"utf-8\") as f:\n            json.dump([asdict(m) for m in meseros], f, indent=2, ensure_ascii=False)\n</code></pre>"},{"location":"reference/#src.mi_app.storage.MeseroJSONStorage.__init__","title":"<code>__init__(filepath)</code>","text":"<p>Inicializa el storage con la ruta del archivo JSON.</p> Source code in <code>src/mi_app/storage.py</code> <pre><code>def __init__(self, filepath: Path) -&gt; None:\n    \"\"\"Inicializa el storage con la ruta del archivo JSON.\"\"\"\n    self.filepath = filepath\n</code></pre>"},{"location":"reference/#src.mi_app.storage.MeseroJSONStorage.cargar","title":"<code>cargar()</code>","text":"<p>Carga la lista de meseros desde el archivo JSON.</p> Source code in <code>src/mi_app/storage.py</code> <pre><code>def cargar(self) -&gt; list[Mesero]:\n    \"\"\"Carga la lista de meseros desde el archivo JSON.\"\"\"\n    if not self.filepath.exists():\n        return []\n\n    with open(self.filepath, \"r\", encoding=\"utf-8\") as f:\n        data = json.load(f)\n\n    return [Mesero(**item) for item in data]\n</code></pre>"},{"location":"reference/#src.mi_app.storage.MeseroJSONStorage.guardar","title":"<code>guardar(meseros)</code>","text":"<p>Persiste la lista de meseros en el archivo JSON.</p> <p>Asigna autom\u00e1ticamente un <code>mesero_id</code> a los registros nuevos cuyo id todav\u00eda sea <code>None</code>, tomando como base el mayor id existente.</p> Source code in <code>src/mi_app/storage.py</code> <pre><code>def guardar(self, meseros: list[Mesero]) -&gt; None:\n    \"\"\"Persiste la lista de meseros en el archivo JSON.\n\n    Asigna autom\u00e1ticamente un ``mesero_id`` a los registros nuevos\n    cuyo id todav\u00eda sea ``None``, tomando como base el mayor id existente.\n    \"\"\"\n    ids_asignados = {m.mesero_id for m in meseros if m.mesero_id is not None}\n    siguiente_id = max(ids_asignados, default=0) + 1\n    for m in meseros:\n        if m.mesero_id is None:\n            m.mesero_id = siguiente_id\n            siguiente_id += 1\n\n    with open(self.filepath, \"w\", encoding=\"utf-8\") as f:\n        json.dump([asdict(m) for m in meseros], f, indent=2, ensure_ascii=False)\n</code></pre>"},{"location":"reference/#platillojsonstorage","title":"PlatilloJSONStorage","text":""},{"location":"reference/#src.mi_app.storage.PlatilloJSONStorage","title":"<code>src.mi_app.storage.PlatilloJSONStorage</code>","text":"<p>Lee y escribe platillos desde/hacia un archivo JSON.</p> Source code in <code>src/mi_app/storage.py</code> <pre><code>class PlatilloJSONStorage:\n    \"\"\"Lee y escribe platillos desde/hacia un archivo JSON.\"\"\"\n\n    def __init__(self, filepath: Path) -&gt; None:\n        \"\"\"Inicializa el storage con la ruta del archivo JSON.\"\"\"\n        self.filepath = filepath\n\n    def cargar(self) -&gt; list[Platillo]:\n        \"\"\"Carga la lista de platillos desde el archivo JSON.\"\"\"\n        if not self.filepath.exists():\n            return []\n\n        with open(self.filepath, \"r\", encoding=\"utf-8\") as f:\n            data = json.load(f)\n\n        return [Platillo(**item) for item in data]\n\n    def guardar(self, platillos: list[Platillo]) -&gt; None:\n        \"\"\"Persiste la lista de platillos en el archivo JSON.\n\n        Asigna autom\u00e1ticamente un ``platillo_id`` a los registros nuevos\n        cuyo id todav\u00eda sea ``None``, tomando como base el mayor id existente.\n        \"\"\"\n        ids_asignados = {p.platillo_id for p in platillos if p.platillo_id is not None}\n        siguiente_id = max(ids_asignados, default=0) + 1\n        for p in platillos:\n            if p.platillo_id is None:\n                p.platillo_id = siguiente_id\n                siguiente_id += 1\n\n        with open(self.filepath, \"w\", encoding=\"utf-8\") as f:\n            json.dump([asdict(p) for p in platillos], f, indent=2, ensure_ascii=False)\n</code></pre>"},{"location":"reference/#src.mi_app.storage.PlatilloJSONStorage.__init__","title":"<code>__init__(filepath)</code>","text":"<p>Inicializa el storage con la ruta del archivo JSON.</p> Source code in <code>src/mi_app/storage.py</code> <pre><code>def __init__(self, filepath: Path) -&gt; None:\n    \"\"\"Inicializa el storage con la ruta del archivo JSON.\"\"\"\n    self.filepath = filepath\n</code></pre>"},{"location":"reference/#src.mi_app.storage.PlatilloJSONStorage.cargar","title":"<code>cargar()</code>","text":"<p>Carga la lista de platillos desde el archivo JSON.</p> Source code in <code>src/mi_app/storage.py</code> <pre><code>def cargar(self) -&gt; list[Platillo]:\n    \"\"\"Carga la lista de platillos desde el archivo JSON.\"\"\"\n    if not self.filepath.exists():\n        return []\n\n    with open(self.filepath, \"r\", encoding=\"utf-8\") as f:\n        data = json.load(f)\n\n    return [Platillo(**item) for item in data]\n</code></pre>"},{"location":"reference/#src.mi_app.storage.PlatilloJSONStorage.guardar","title":"<code>guardar(platillos)</code>","text":"<p>Persiste la lista de platillos en el archivo JSON.</p> <p>Asigna autom\u00e1ticamente un <code>platillo_id</code> a los registros nuevos cuyo id todav\u00eda sea <code>None</code>, tomando como base el mayor id existente.</p> Source code in <code>src/mi_app/storage.py</code> <pre><code>def guardar(self, platillos: list[Platillo]) -&gt; None:\n    \"\"\"Persiste la lista de platillos en el archivo JSON.\n\n    Asigna autom\u00e1ticamente un ``platillo_id`` a los registros nuevos\n    cuyo id todav\u00eda sea ``None``, tomando como base el mayor id existente.\n    \"\"\"\n    ids_asignados = {p.platillo_id for p in platillos if p.platillo_id is not None}\n    siguiente_id = max(ids_asignados, default=0) + 1\n    for p in platillos:\n        if p.platillo_id is None:\n            p.platillo_id = siguiente_id\n            siguiente_id += 1\n\n    with open(self.filepath, \"w\", encoding=\"utf-8\") as f:\n        json.dump([asdict(p) for p in platillos], f, indent=2, ensure_ascii=False)\n</code></pre>"},{"location":"reference/#storage-supabase-nube","title":"Storage \u2014 Supabase (nube)","text":""},{"location":"reference/#meserosupabasestorage","title":"MeseroSupabaseStorage","text":""},{"location":"reference/#src.mi_app.storage_supabase.MeseroSupabaseStorage","title":"<code>src.mi_app.storage_supabase.MeseroSupabaseStorage</code>","text":"<p>Lee y escribe meseros usando la API REST de Supabase (PostgREST).</p> <p>Implementa el mismo contrato que <code>MeseroJSONStorage</code>; los servicios no distinguen entre una u otra implementaci\u00f3n.</p> Operaciones <ul> <li><code>cargar</code>  \u2192 GET  /rest/v1/meseros</li> <li><code>guardar</code> \u2192 POST /rest/v1/meseros (INSERT o UPSERT) + DELETE de hu\u00e9rfanos</li> </ul> Source code in <code>src/mi_app/storage_supabase.py</code> <pre><code>class MeseroSupabaseStorage:\n    \"\"\"Lee y escribe meseros usando la API REST de Supabase (PostgREST).\n\n    Implementa el mismo contrato que ``MeseroJSONStorage``; los servicios\n    no distinguen entre una u otra implementaci\u00f3n.\n\n    Operaciones:\n        - ``cargar``  \u2192 GET  /rest/v1/meseros\n        - ``guardar`` \u2192 POST /rest/v1/meseros (INSERT o UPSERT) + DELETE de hu\u00e9rfanos\n    \"\"\"\n\n    def cargar(self) -&gt; list[Mesero]:\n        \"\"\"Carga la lista de meseros desde la tabla ``meseros`` en Supabase.\"\"\"\n        resp = requests.get(\n            f\"{_base()}/meseros\",\n            headers=_headers(),\n            params={\"order\": \"mesero_id.asc\"},\n        )\n        _check(resp)\n        return [Mesero(**row) for row in resp.json()]\n\n    def guardar(self, meseros: list[Mesero]) -&gt; None:\n        \"\"\"Sincroniza la lista de meseros con la tabla ``meseros`` en Supabase.\n\n        Los meseros sin ``mesero_id`` se insertan como registros nuevos; la columna\n        SERIAL de PostgreSQL genera el ID y se asigna de vuelta al objeto en memoria.\n        Los meseros con ID existente se actualizan mediante UPSERT.\n        Al final se eliminan los registros que ya no forman parte de la lista.\n        \"\"\"\n        base = _base()\n\n        # Separar antes de que los nuevos reciban su ID\n        nuevos = [m for m in meseros if m.mesero_id is None]\n        existentes = [m for m in meseros if m.mesero_id is not None]\n\n        # INSERT de nuevos registros; PostgreSQL asigna el SERIAL\n        if nuevos:\n            resp = requests.post(\n                f\"{base}/meseros\",\n                headers=_headers(\"return=representation\"),\n                json=[{\"nombre\": m.nombre, \"pin\": m.pin} for m in nuevos],\n            )\n            _check(resp)\n            for m, row in zip(nuevos, resp.json()):\n                m.mesero_id = row[\"mesero_id\"]\n\n        # UPSERT de registros que ya tienen ID (actualiza campos si cambiaron)\n        if existentes:\n            upsert_resp = requests.post(\n                f\"{base}/meseros\",\n                headers=_headers(\"resolution=merge-duplicates,return=minimal\"),\n                json=[\n                    {\"mesero_id\": m.mesero_id, \"nombre\": m.nombre, \"pin\": m.pin}\n                    for m in existentes\n                ],\n            )\n            _check(upsert_resp)\n\n        # Eliminar registros que ya no est\u00e1n en la lista\n        ids_actuales = [m.mesero_id for m in meseros]\n        if ids_actuales:\n            ids_str = \",\".join(str(mid) for mid in ids_actuales)\n            del_resp = requests.delete(\n                f\"{base}/meseros?mesero_id=not.in.({ids_str})\",\n                headers=_headers(\"return=minimal\"),\n            )\n            _check(del_resp)\n        else:\n            # Lista vac\u00eda: borrar todos; se filtra por gte.0 porque la API\n            # requiere al menos un filtro para operaciones de borrado masivo\n            del_resp = requests.delete(\n                f\"{base}/meseros?mesero_id=gte.0\",\n                headers=_headers(\"return=minimal\"),\n            )\n            _check(del_resp)\n</code></pre>"},{"location":"reference/#src.mi_app.storage_supabase.MeseroSupabaseStorage.cargar","title":"<code>cargar()</code>","text":"<p>Carga la lista de meseros desde la tabla <code>meseros</code> en Supabase.</p> Source code in <code>src/mi_app/storage_supabase.py</code> <pre><code>def cargar(self) -&gt; list[Mesero]:\n    \"\"\"Carga la lista de meseros desde la tabla ``meseros`` en Supabase.\"\"\"\n    resp = requests.get(\n        f\"{_base()}/meseros\",\n        headers=_headers(),\n        params={\"order\": \"mesero_id.asc\"},\n    )\n    _check(resp)\n    return [Mesero(**row) for row in resp.json()]\n</code></pre>"},{"location":"reference/#src.mi_app.storage_supabase.MeseroSupabaseStorage.guardar","title":"<code>guardar(meseros)</code>","text":"<p>Sincroniza la lista de meseros con la tabla <code>meseros</code> en Supabase.</p> <p>Los meseros sin <code>mesero_id</code> se insertan como registros nuevos; la columna SERIAL de PostgreSQL genera el ID y se asigna de vuelta al objeto en memoria. Los meseros con ID existente se actualizan mediante UPSERT. Al final se eliminan los registros que ya no forman parte de la lista.</p> Source code in <code>src/mi_app/storage_supabase.py</code> <pre><code>def guardar(self, meseros: list[Mesero]) -&gt; None:\n    \"\"\"Sincroniza la lista de meseros con la tabla ``meseros`` en Supabase.\n\n    Los meseros sin ``mesero_id`` se insertan como registros nuevos; la columna\n    SERIAL de PostgreSQL genera el ID y se asigna de vuelta al objeto en memoria.\n    Los meseros con ID existente se actualizan mediante UPSERT.\n    Al final se eliminan los registros que ya no forman parte de la lista.\n    \"\"\"\n    base = _base()\n\n    # Separar antes de que los nuevos reciban su ID\n    nuevos = [m for m in meseros if m.mesero_id is None]\n    existentes = [m for m in meseros if m.mesero_id is not None]\n\n    # INSERT de nuevos registros; PostgreSQL asigna el SERIAL\n    if nuevos:\n        resp = requests.post(\n            f\"{base}/meseros\",\n            headers=_headers(\"return=representation\"),\n            json=[{\"nombre\": m.nombre, \"pin\": m.pin} for m in nuevos],\n        )\n        _check(resp)\n        for m, row in zip(nuevos, resp.json()):\n            m.mesero_id = row[\"mesero_id\"]\n\n    # UPSERT de registros que ya tienen ID (actualiza campos si cambiaron)\n    if existentes:\n        upsert_resp = requests.post(\n            f\"{base}/meseros\",\n            headers=_headers(\"resolution=merge-duplicates,return=minimal\"),\n            json=[\n                {\"mesero_id\": m.mesero_id, \"nombre\": m.nombre, \"pin\": m.pin}\n                for m in existentes\n            ],\n        )\n        _check(upsert_resp)\n\n    # Eliminar registros que ya no est\u00e1n en la lista\n    ids_actuales = [m.mesero_id for m in meseros]\n    if ids_actuales:\n        ids_str = \",\".join(str(mid) for mid in ids_actuales)\n        del_resp = requests.delete(\n            f\"{base}/meseros?mesero_id=not.in.({ids_str})\",\n            headers=_headers(\"return=minimal\"),\n        )\n        _check(del_resp)\n    else:\n        # Lista vac\u00eda: borrar todos; se filtra por gte.0 porque la API\n        # requiere al menos un filtro para operaciones de borrado masivo\n        del_resp = requests.delete(\n            f\"{base}/meseros?mesero_id=gte.0\",\n            headers=_headers(\"return=minimal\"),\n        )\n        _check(del_resp)\n</code></pre>"},{"location":"reference/#platillosupabasestorage","title":"PlatilloSupabaseStorage","text":""},{"location":"reference/#src.mi_app.storage_supabase.PlatilloSupabaseStorage","title":"<code>src.mi_app.storage_supabase.PlatilloSupabaseStorage</code>","text":"<p>Lee y escribe platillos usando la API REST de Supabase (PostgREST).</p> <p>Implementa el mismo contrato que <code>PlatilloJSONStorage</code>; los servicios no distinguen entre una u otra implementaci\u00f3n.</p> Operaciones <ul> <li><code>cargar</code>  \u2192 GET  /rest/v1/platillos</li> <li><code>guardar</code> \u2192 POST /rest/v1/platillos (INSERT o UPSERT) + DELETE de hu\u00e9rfanos</li> </ul> Source code in <code>src/mi_app/storage_supabase.py</code> <pre><code>class PlatilloSupabaseStorage:\n    \"\"\"Lee y escribe platillos usando la API REST de Supabase (PostgREST).\n\n    Implementa el mismo contrato que ``PlatilloJSONStorage``; los servicios\n    no distinguen entre una u otra implementaci\u00f3n.\n\n    Operaciones:\n        - ``cargar``  \u2192 GET  /rest/v1/platillos\n        - ``guardar`` \u2192 POST /rest/v1/platillos (INSERT o UPSERT) + DELETE de hu\u00e9rfanos\n    \"\"\"\n\n    def cargar(self) -&gt; list[Platillo]:\n        \"\"\"Carga la lista de platillos desde la tabla ``platillos`` en Supabase.\"\"\"\n        resp = requests.get(\n            f\"{_base()}/platillos\",\n            headers=_headers(),\n            params={\"order\": \"platillo_id.asc\"},\n        )\n        _check(resp)\n        return [\n            Platillo(\n                platillo_id=row[\"platillo_id\"],\n                nombre=row[\"nombre\"],\n                # precio viene como Decimal de PostgreSQL; se convierte a float\n                precio=float(row[\"precio\"]),\n                categoria=row[\"categoria\"],\n                disponible=row[\"disponible\"],\n            )\n            for row in resp.json()\n        ]\n\n    def guardar(self, platillos: list[Platillo]) -&gt; None:\n        \"\"\"Sincroniza la lista de platillos con la tabla ``platillos`` en Supabase.\n\n        Los platillos sin ``platillo_id`` se insertan como registros nuevos; la columna\n        SERIAL de PostgreSQL genera el ID y se asigna de vuelta al objeto en memoria.\n        Los platillos con ID existente se actualizan mediante UPSERT.\n        Al final se eliminan los registros que ya no forman parte de la lista.\n        \"\"\"\n        base = _base()\n\n        nuevos = [p for p in platillos if p.platillo_id is None]\n        existentes = [p for p in platillos if p.platillo_id is not None]\n\n        # INSERT de nuevos registros; PostgreSQL asigna el SERIAL\n        if nuevos:\n            resp = requests.post(\n                f\"{base}/platillos\",\n                headers=_headers(\"return=representation\"),\n                json=[\n                    {\n                        \"nombre\": p.nombre,\n                        \"precio\": p.precio,\n                        \"categoria\": p.categoria,\n                        \"disponible\": p.disponible,\n                    }\n                    for p in nuevos\n                ],\n            )\n            _check(resp)\n            for p, row in zip(nuevos, resp.json()):\n                p.platillo_id = row[\"platillo_id\"]\n\n        # UPSERT de registros que ya tienen ID\n        if existentes:\n            upsert_resp = requests.post(\n                f\"{base}/platillos\",\n                headers=_headers(\"resolution=merge-duplicates,return=minimal\"),\n                json=[\n                    {\n                        \"platillo_id\": p.platillo_id,\n                        \"nombre\": p.nombre,\n                        \"precio\": p.precio,\n                        \"categoria\": p.categoria,\n                        \"disponible\": p.disponible,\n                    }\n                    for p in existentes\n                ],\n            )\n            _check(upsert_resp)\n\n        # Eliminar registros que ya no est\u00e1n en la lista\n        ids_actuales = [p.platillo_id for p in platillos]\n        if ids_actuales:\n            ids_str = \",\".join(str(pid) for pid in ids_actuales)\n            del_resp = requests.delete(\n                f\"{base}/platillos?platillo_id=not.in.({ids_str})\",\n                headers=_headers(\"return=minimal\"),\n            )\n            _check(del_resp)\n        else:\n            del_resp = requests.delete(\n                f\"{base}/platillos?platillo_id=gte.0\",\n                headers=_headers(\"return=minimal\"),\n            )\n            _check(del_resp)\n</code></pre>"},{"location":"reference/#src.mi_app.storage_supabase.PlatilloSupabaseStorage.cargar","title":"<code>cargar()</code>","text":"<p>Carga la lista de platillos desde la tabla <code>platillos</code> en Supabase.</p> Source code in <code>src/mi_app/storage_supabase.py</code> <pre><code>def cargar(self) -&gt; list[Platillo]:\n    \"\"\"Carga la lista de platillos desde la tabla ``platillos`` en Supabase.\"\"\"\n    resp = requests.get(\n        f\"{_base()}/platillos\",\n        headers=_headers(),\n        params={\"order\": \"platillo_id.asc\"},\n    )\n    _check(resp)\n    return [\n        Platillo(\n            platillo_id=row[\"platillo_id\"],\n            nombre=row[\"nombre\"],\n            # precio viene como Decimal de PostgreSQL; se convierte a float\n            precio=float(row[\"precio\"]),\n            categoria=row[\"categoria\"],\n            disponible=row[\"disponible\"],\n        )\n        for row in resp.json()\n    ]\n</code></pre>"},{"location":"reference/#src.mi_app.storage_supabase.PlatilloSupabaseStorage.guardar","title":"<code>guardar(platillos)</code>","text":"<p>Sincroniza la lista de platillos con la tabla <code>platillos</code> en Supabase.</p> <p>Los platillos sin <code>platillo_id</code> se insertan como registros nuevos; la columna SERIAL de PostgreSQL genera el ID y se asigna de vuelta al objeto en memoria. Los platillos con ID existente se actualizan mediante UPSERT. Al final se eliminan los registros que ya no forman parte de la lista.</p> Source code in <code>src/mi_app/storage_supabase.py</code> <pre><code>def guardar(self, platillos: list[Platillo]) -&gt; None:\n    \"\"\"Sincroniza la lista de platillos con la tabla ``platillos`` en Supabase.\n\n    Los platillos sin ``platillo_id`` se insertan como registros nuevos; la columna\n    SERIAL de PostgreSQL genera el ID y se asigna de vuelta al objeto en memoria.\n    Los platillos con ID existente se actualizan mediante UPSERT.\n    Al final se eliminan los registros que ya no forman parte de la lista.\n    \"\"\"\n    base = _base()\n\n    nuevos = [p for p in platillos if p.platillo_id is None]\n    existentes = [p for p in platillos if p.platillo_id is not None]\n\n    # INSERT de nuevos registros; PostgreSQL asigna el SERIAL\n    if nuevos:\n        resp = requests.post(\n            f\"{base}/platillos\",\n            headers=_headers(\"return=representation\"),\n            json=[\n                {\n                    \"nombre\": p.nombre,\n                    \"precio\": p.precio,\n                    \"categoria\": p.categoria,\n                    \"disponible\": p.disponible,\n                }\n                for p in nuevos\n            ],\n        )\n        _check(resp)\n        for p, row in zip(nuevos, resp.json()):\n            p.platillo_id = row[\"platillo_id\"]\n\n    # UPSERT de registros que ya tienen ID\n    if existentes:\n        upsert_resp = requests.post(\n            f\"{base}/platillos\",\n            headers=_headers(\"resolution=merge-duplicates,return=minimal\"),\n            json=[\n                {\n                    \"platillo_id\": p.platillo_id,\n                    \"nombre\": p.nombre,\n                    \"precio\": p.precio,\n                    \"categoria\": p.categoria,\n                    \"disponible\": p.disponible,\n                }\n                for p in existentes\n            ],\n        )\n        _check(upsert_resp)\n\n    # Eliminar registros que ya no est\u00e1n en la lista\n    ids_actuales = [p.platillo_id for p in platillos]\n    if ids_actuales:\n        ids_str = \",\".join(str(pid) for pid in ids_actuales)\n        del_resp = requests.delete(\n            f\"{base}/platillos?platillo_id=not.in.({ids_str})\",\n            headers=_headers(\"return=minimal\"),\n        )\n        _check(del_resp)\n    else:\n        del_resp = requests.delete(\n            f\"{base}/platillos?platillo_id=gte.0\",\n            headers=_headers(\"return=minimal\"),\n        )\n        _check(del_resp)\n</code></pre>"},{"location":"reference/#excepciones","title":"Excepciones","text":""},{"location":"reference/#src.mi_app.exceptions","title":"<code>src.mi_app.exceptions</code>","text":""},{"location":"reference/#src.mi_app.exceptions.AppError","title":"<code>AppError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Base class for all application-specific exceptions.</p> Source code in <code>src/mi_app/exceptions.py</code> <pre><code>class AppError(Exception):\n    \"\"\"Base class for all application-specific exceptions.\"\"\"\n\n    pass\n</code></pre>"},{"location":"reference/#src.mi_app.exceptions.CategoriaInvalidaError","title":"<code>CategoriaInvalidaError</code>","text":"<p>               Bases: <code>PlatilloError</code></p> <p>Se lanza cuando se usa una categor\u00eda que no est\u00e1 permitida.</p> Source code in <code>src/mi_app/exceptions.py</code> <pre><code>class CategoriaInvalidaError(PlatilloError):\n    \"\"\"Se lanza cuando se usa una categor\u00eda que no est\u00e1 permitida.\"\"\"\n\n    CATEGORIAS_VALIDAS = {\"entrada\", \"plato_fuerte\", \"postre\", \"bebida\"}\n\n    def __init__(self, categoria: str) -&gt; None:\n        self.categoria = categoria\n        super().__init__(f\"Categor\u00eda '{categoria}' no v\u00e1lida. Opciones: {self.CATEGORIAS_VALIDAS}\")\n</code></pre>"},{"location":"reference/#src.mi_app.exceptions.DatosMeseroInvalidosError","title":"<code>DatosMeseroInvalidosError</code>","text":"<p>               Bases: <code>MeseroError</code></p> <p>Se lanza cuando los datos de un mesero no pasan la validaci\u00f3n.</p> Source code in <code>src/mi_app/exceptions.py</code> <pre><code>class DatosMeseroInvalidosError(MeseroError):\n    \"\"\"Se lanza cuando los datos de un mesero no pasan la validaci\u00f3n.\"\"\"\n\n    def __init__(self, mensaje: str) -&gt; None:\n        super().__init__(mensaje)\n</code></pre>"},{"location":"reference/#src.mi_app.exceptions.DatosPlatilloInvalidosError","title":"<code>DatosPlatilloInvalidosError</code>","text":"<p>               Bases: <code>PlatilloError</code></p> <p>Se lanza cuando los datos de un platillo no pasan la validaci\u00f3n.</p> Source code in <code>src/mi_app/exceptions.py</code> <pre><code>class DatosPlatilloInvalidosError(PlatilloError):\n    \"\"\"Se lanza cuando los datos de un platillo no pasan la validaci\u00f3n.\"\"\"\n\n    def __init__(self, mensaje: str) -&gt; None:\n        super().__init__(mensaje)\n</code></pre>"},{"location":"reference/#src.mi_app.exceptions.MeseroError","title":"<code>MeseroError</code>","text":"<p>               Bases: <code>AppError</code></p> <p>Clase base para mesero-related exceptions.</p> Source code in <code>src/mi_app/exceptions.py</code> <pre><code>class MeseroError(AppError):\n    \"\"\"Clase base para mesero-related exceptions.\"\"\"\n\n    pass\n</code></pre>"},{"location":"reference/#src.mi_app.exceptions.MeseroNoEncontradoError","title":"<code>MeseroNoEncontradoError</code>","text":"<p>               Bases: <code>MeseroError</code></p> <p>Se lanza cuando no se encuentra un mesero con el id dado.</p> Source code in <code>src/mi_app/exceptions.py</code> <pre><code>class MeseroNoEncontradoError(MeseroError):\n    \"\"\"Se lanza cuando no se encuentra un mesero con el id dado.\"\"\"\n\n    def __init__(self, mesero_id: int) -&gt; None:\n        self.mesero_id = mesero_id\n        super().__init__(f\"Mesero con id {mesero_id} no encontrado\")\n</code></pre>"},{"location":"reference/#src.mi_app.exceptions.MeseroYaExisteError","title":"<code>MeseroYaExisteError</code>","text":"<p>               Bases: <code>MeseroError</code></p> <p>Se lanza cuando se intenta registrar un mesero con un id ya existente.</p> Source code in <code>src/mi_app/exceptions.py</code> <pre><code>class MeseroYaExisteError(MeseroError):\n    \"\"\"Se lanza cuando se intenta registrar un mesero con un id ya existente.\"\"\"\n\n    def __init__(self, mesero_id: int) -&gt; None:\n        self.mesero_id = mesero_id\n        super().__init__(f\"Mesero con id {mesero_id} ya existe\")\n</code></pre>"},{"location":"reference/#src.mi_app.exceptions.PlatilloError","title":"<code>PlatilloError</code>","text":"<p>               Bases: <code>AppError</code></p> <p>Clase Base para platillo-related exceptions.</p> Source code in <code>src/mi_app/exceptions.py</code> <pre><code>class PlatilloError(AppError):\n    \"\"\"Clase Base para platillo-related exceptions.\"\"\"\n\n    pass\n</code></pre>"},{"location":"reference/#src.mi_app.exceptions.PlatilloNoEncontradoError","title":"<code>PlatilloNoEncontradoError</code>","text":"<p>               Bases: <code>PlatilloError</code></p> <p>Se lanza cuando no se encuentra un platillo con el id dado.</p> Source code in <code>src/mi_app/exceptions.py</code> <pre><code>class PlatilloNoEncontradoError(PlatilloError):\n    \"\"\"Se lanza cuando no se encuentra un platillo con el id dado.\"\"\"\n\n    def __init__(self, platillo_id: int) -&gt; None:\n        self.platillo_id = platillo_id\n        super().__init__(f\"Platillo con id {platillo_id} no encontrado\")\n</code></pre>"},{"location":"reference/#src.mi_app.exceptions.PlatilloYaExisteError","title":"<code>PlatilloYaExisteError</code>","text":"<p>               Bases: <code>PlatilloError</code></p> <p>Se lanza cuando se intenta crear un platillo con un id ya existente.</p> Source code in <code>src/mi_app/exceptions.py</code> <pre><code>class PlatilloYaExisteError(PlatilloError):\n    \"\"\"Se lanza cuando se intenta crear un platillo con un id ya existente.\"\"\"\n\n    def __init__(self, platillo_id: int) -&gt; None:\n        self.platillo_id = platillo_id\n        super().__init__(f\"Platillo con id {platillo_id} ya existe\")\n</code></pre>"},{"location":"user-guide/commands/","title":"\ud83d\udda5\ufe0f Comandos CLI","text":"<p>Restaurante CLI ofrece dos formas de interacci\u00f3n: un men\u00fa interactivo y comandos directos con flags.</p>"},{"location":"user-guide/commands/#menu-interactivo","title":"Men\u00fa interactivo","text":"<pre><code>uv run python main.py menu\n</code></pre> <p>Abre una interfaz en la terminal donde puedes navegar entre opciones usando n\u00fameros.</p> <p>Recomendado para uso r\u00e1pido</p> <p>El men\u00fa interactivo solicita todos los datos paso a paso con prompts.</p>"},{"location":"user-guide/commands/#comandos-de-meseros","title":"Comandos de Meseros","text":""},{"location":"user-guide/commands/#registrar-un-mesero","title":"Registrar un mesero","text":"Men\u00fa interactivoComando directo <p>Selecciona la opci\u00f3n 1 y sigue los prompts.</p> <pre><code>uv run python main.py meseros registrar --nombre \"Carlos P\u00e9rez\" --pin 1234\n</code></pre> <p>El sistema asigna el ID autom\u00e1ticamente y lo muestra al finalizar.</p> <p>Par\u00e1metros:</p> Par\u00e1metro Tipo Descripci\u00f3n <code>--nombre</code> <code>str</code> Nombre completo <code>--pin</code> <code>str</code> PIN de 4 d\u00edgitos num\u00e9ricos <p>Ejemplo de salida:</p> <pre><code>\u256d\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256e\n\u2502 \u2705 Mesero Carlos P\u00e9rez registrado con ID 1        \u2502\n\u2570\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256f\n</code></pre>"},{"location":"user-guide/commands/#login-de-mesero","title":"Login de mesero","text":"Men\u00fa interactivoComando directo <p>Selecciona la opci\u00f3n 2.</p> <pre><code>uv run python main.py meseros login --id 1 --pin 1234\n</code></pre>"},{"location":"user-guide/commands/#listar-meseros","title":"Listar meseros","text":"<pre><code>uv run python main.py meseros listar\n</code></pre> <p>Muestra una tabla con todos los meseros registrados. El PIN se muestra oculto con <code>\u25cf\u25cf\u25cf\u25cf</code>.</p>"},{"location":"user-guide/commands/#buscar-mesero-por-id","title":"Buscar mesero por ID","text":"<pre><code>uv run python main.py meseros obtener --id 1\n</code></pre>"},{"location":"user-guide/commands/#eliminar-mesero","title":"Eliminar mesero","text":"<pre><code>uv run python main.py meseros eliminar --id 1\n</code></pre> <p>Acci\u00f3n irreversible</p> <p>Una vez eliminado, el mesero no puede recuperarse.</p>"},{"location":"user-guide/commands/#comandos-de-platillos","title":"Comandos de Platillos","text":""},{"location":"user-guide/commands/#crear-un-platillo","title":"Crear un platillo","text":"Men\u00fa interactivoComando directo <p>Selecciona la opci\u00f3n 6 y sigue los prompts.</p> <pre><code>uv run python main.py platillos crear --nombre \"Sopa del d\u00eda\" --precio 12000 --categoria entrada\n</code></pre> <p>El sistema asigna el ID autom\u00e1ticamente y lo muestra al finalizar.</p> <p>Par\u00e1metros:</p> Par\u00e1metro Tipo Descripci\u00f3n <code>--nombre</code> <code>str</code> Nombre del platillo <code>--precio</code> <code>float</code> Precio en pesos colombianos <code>--categoria</code> <code>str</code> <code>entrada</code>, <code>plato_fuerte</code>, <code>postre</code> o <code>bebida</code>"},{"location":"user-guide/commands/#listar-menu-completo","title":"Listar men\u00fa completo","text":"<pre><code>uv run python main.py platillos listar\n</code></pre>"},{"location":"user-guide/commands/#filtrar-por-categoria","title":"Filtrar por categor\u00eda","text":"<pre><code>uv run python main.py platillos listar --categoria entrada\n</code></pre>"},{"location":"user-guide/commands/#buscar-platillo-por-id","title":"Buscar platillo por ID","text":"<pre><code>uv run python main.py platillos obtener --id 1\n</code></pre>"},{"location":"user-guide/commands/#actualizar-platillo","title":"Actualizar platillo","text":"<pre><code>uv run python main.py platillos actualizar --id 1 --nombre \"Sopa criolla\" --precio 15000 --categoria entrada\n</code></pre>"},{"location":"user-guide/commands/#eliminar-platillo","title":"Eliminar platillo","text":"<pre><code>uv run python main.py platillos eliminar --id 1\n</code></pre> <p>Acci\u00f3n irreversible</p> <p>El platillo se eliminar\u00e1 permanentemente del men\u00fa.</p>"},{"location":"user-guide/persistence/","title":"\ud83d\udcbe Persistencia de datos","text":"<p>El sistema soporta dos mecanismos de persistencia intercambiables: archivos JSON locales y una base de datos PostgreSQL alojada en Supabase. El modo activo se determina autom\u00e1ticamente al iniciar la aplicaci\u00f3n seg\u00fan las variables de entorno configuradas.</p>"},{"location":"user-guide/persistence/#seleccion-automatica-del-backend","title":"Selecci\u00f3n autom\u00e1tica del backend","text":"<p>Al arrancar, los m\u00f3dulos CLI comprueban si las variables <code>SUPABASE_URL</code> y <code>SUPABASE_KEY</code> est\u00e1n definidas en el entorno (cargadas desde el archivo <code>.env</code>):</p> <pre><code>def _build_storage():\n    if os.environ.get(\"SUPABASE_URL\") and os.environ.get(\"SUPABASE_KEY\"):\n        return MeseroSupabaseStorage()          # \u2601\ufe0f  Supabase\n    return MeseroJSONStorage(Path(\"data/meseros.json\"))  # \ud83d\udcc4 JSON local\n</code></pre> Condici\u00f3n Backend activo Archivo usado Sin <code>.env</code> JSON local <code>data/meseros.json</code>, <code>data/platillos.json</code> Con <code>.env</code> y claves v\u00e1lidas Supabase REST API Tablas <code>meseros</code>, <code>platillos</code> en PostgreSQL"},{"location":"user-guide/persistence/#modo-json-local","title":"Modo JSON (local)","text":""},{"location":"user-guide/persistence/#ubicacion-de-los-archivos","title":"Ubicaci\u00f3n de los archivos","text":"<p>Los datos se almacenan en la carpeta <code>data/</code> en la ra\u00edz del proyecto:</p> <pre><code>data/\n\u251c\u2500\u2500 meseros.json\n\u2514\u2500\u2500 platillos.json\n</code></pre> <p>Creaci\u00f3n autom\u00e1tica</p> <p>Si los archivos no existen al iniciar la aplicaci\u00f3n, el sistema los crea autom\u00e1ticamente al realizar la primera operaci\u00f3n de escritura.</p>"},{"location":"user-guide/persistence/#estructura-de-los-datos","title":"Estructura de los datos","text":""},{"location":"user-guide/persistence/#meserosjson","title":"meseros.json","text":"<pre><code>[\n  { \"mesero_id\": 1, \"nombre\": \"Carlos P\u00e9rez\", \"pin\": \"1234\" },\n  { \"mesero_id\": 2, \"nombre\": \"Mar\u00eda L\u00f3pez\",  \"pin\": \"5678\" }\n]\n</code></pre>"},{"location":"user-guide/persistence/#platillosjson","title":"platillos.json","text":"<pre><code>[\n  {\n    \"platillo_id\": 1,\n    \"nombre\": \"Sopa del d\u00eda\",\n    \"precio\": 12000.0,\n    \"categoria\": \"entrada\",\n    \"disponible\": true\n  }\n]\n</code></pre>"},{"location":"user-guide/persistence/#como-funciona-la-serializacion","title":"C\u00f3mo funciona la serializaci\u00f3n","text":"<p>Los modelos est\u00e1n implementados con <code>dataclasses</code>, lo que permite convertirlos f\u00e1cilmente a diccionarios usando <code>dataclasses.asdict()</code>.</p> <pre><code>flowchart LR\n    DC[\"@dataclass\\nMesero / Platillo\"]\n    DICT[\"dict (asdict)\"]\n    JSON[\"\ud83d\udcc4 archivo .json\"]\n\n    DC --&gt;|\"asdict()\"| DICT --&gt;|\"json.dump()\"| JSON\n    JSON --&gt;|\"json.load()\"| DICT --&gt;|\"Mesero(**d)\"| DC</code></pre>"},{"location":"user-guide/persistence/#modo-supabase-postgresql-en-la-nube","title":"Modo Supabase (PostgreSQL en la nube)","text":""},{"location":"user-guide/persistence/#configuracion-del-entorno","title":"Configuraci\u00f3n del entorno","text":"<ol> <li> <p>Crea el archivo <code>.env</code> en la ra\u00edz del proyecto (ya est\u00e1 incluido en <code>.gitignore</code>):</p> <pre><code># .env\nSUPABASE_URL=https://[TU_REF].supabase.co\nSUPABASE_KEY=[TU_ANON_KEY_O_SERVICE_ROLE_KEY]\n</code></pre> </li> <li> <p>Las claves se obtienen en Supabase \u2192 Settings \u2192 API.</p> </li> </ol> <p>Permisos de escritura</p> <p>Si usas la anon key, aseg\u00farate de que las pol\u00edticas RLS (Row Level Security) de Supabase permitan operaciones <code>INSERT</code>, <code>UPDATE</code> y <code>DELETE</code> en las tablas <code>meseros</code> y <code>platillos</code>. La service_role key tiene acceso completo sin necesidad de configurar RLS.</p>"},{"location":"user-guide/persistence/#crear-las-tablas-en-supabase","title":"Crear las tablas en Supabase","text":"<p>Ejecuta el archivo <code>schema.sql</code> (incluido en la ra\u00edz del proyecto) desde el SQL Editor de Supabase. Este script crea las siguientes tablas con todas sus restricciones:</p> Tabla Descripci\u00f3n Clave primaria <code>meseros</code> Empleados del restaurante <code>mesero_id</code> (asignado por el operador) <code>platillos</code> \u00cdtems del men\u00fa <code>platillo_id</code> (asignado por el operador) <code>mesas</code> Mesas f\u00edsicas del restaurante <code>mesa_id</code> (SERIAL) <code>pedidos</code> \u00d3rdenes de los clientes <code>pedido_id</code> (SERIAL) <code>detalle_pedido</code> L\u00edneas de cada pedido <code>detalle_id</code> (SERIAL)"},{"location":"user-guide/persistence/#modelo-entidad-relacion-er","title":"Modelo Entidad-Relaci\u00f3n (ER)","text":"<pre><code>erDiagram\n    meseros {\n        int mesero_id PK\n        varchar nombre\n        char pin\n    }\n    platillos {\n        int platillo_id PK\n        varchar nombre\n        numeric precio\n        varchar categoria\n        boolean disponible\n    }\n    mesas {\n        serial mesa_id PK\n        int numero\n        int capacidad\n        varchar estado\n    }\n    pedidos {\n        serial pedido_id PK\n        int mesa_id FK\n        int mesero_id FK\n        timestamp fecha_hora\n        varchar estado\n        numeric total\n    }\n    detalle_pedido {\n        serial detalle_id PK\n        int pedido_id FK\n        int platillo_id FK\n        int cantidad\n        numeric precio_unitario\n        numeric subtotal\n    }\n\n    mesas    ||--o{ pedidos         : \"tiene\"\n    meseros  ||--o{ pedidos         : \"atiende\"\n    pedidos  ||--|{ detalle_pedido  : \"contiene\"\n    platillos||--o{ detalle_pedido  : \"incluido en\"</code></pre>"},{"location":"user-guide/persistence/#como-funciona-el-almacenamiento-en-supabase","title":"C\u00f3mo funciona el almacenamiento en Supabase","text":"<p>La comunicaci\u00f3n se realiza mediante la API REST de Supabase (PostgREST), con operaciones UPSERT y DELETE selectivo para mantener la tabla sincronizada con el estado en memoria.</p> <pre><code>flowchart LR\n    DC[\"@dataclass\\nMesero / Platillo\"]\n    JSON2[\"JSON payload\"]\n    SB[\"\u2601\ufe0f Supabase\\nREST API\"]\n    PG[\"\ud83d\udc18 PostgreSQL\\ntabla meseros/platillos\"]\n\n    DC --&gt;|\"dict()\"| JSON2 --&gt;|\"POST /rest/v1/\u2026\\nPrefer: merge-duplicates\"| SB --&gt; PG\n    PG --&gt;|\"GET /rest/v1/\u2026\"| SB --&gt;|\"resp.json()\"| JSON2 --&gt;|\"Mesero(**row)\"| DC</code></pre>"},{"location":"user-guide/persistence/#guardar-sincronizacion","title":"Guardar (sincronizaci\u00f3n)","text":"<ol> <li>UPSERT de todos los registros actuales (<code>POST</code> con <code>Prefer: resolution=merge-duplicates</code>).</li> <li>DELETE de los registros cuyo ID ya no est\u00e9 en la lista (mantiene integridad referencial).</li> </ol>"},{"location":"user-guide/persistence/#cargar-lectura","title":"Cargar (lectura)","text":"<ol> <li><code>GET</code> a la tabla correspondiente con orden por ID ascendente.</li> <li>Reconstrucci\u00f3n de los objetos usando <code>Mesero(**row)</code> / <code>Platillo(**row)</code>.</li> <li>Al reconstruir, <code>__post_init__</code> valida autom\u00e1ticamente los datos recibidos.</li> </ol> <p>Contrato de Storage (Protocol)</p> <p>Ambas implementaciones (JSON y Supabase) respetan el mismo <code>Protocol</code>, lo que permite a\u00f1adir futuros backends (SQLite, Redis, etc.) sin cambiar nada en los servicios.</p> <pre><code>class MeseroStorage(Protocol):\n    def cargar(self) -&gt; list[Mesero]: ...\n    def guardar(self, meseros: list[Mesero]) -&gt; None: ...\n</code></pre>"}]}